# Vending Audit Skill

This README is only a pointer; it is not the vending-audit skill.

The required file is `.opencode/skills/vending-audit/SKILL.md`.

- If `SKILL.md` exists, read and review that current participant-authored file.
- If it does not exist, read `SPIRAL-TASK.md` and ask the human participant to
  create it with an editor outside the OpenCode chat.

Never infer that `SKILL.md` is absent from this README or from an earlier
conversation. Check the current filesystem. Restart OpenCode after the human
creates the file so the skill index refreshes.
